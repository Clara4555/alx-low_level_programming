#include "main.h"
/**
  *_islower - func to check for lowercase charcaters
  *@c: the char to check
  *Return: 1 if c is lowercaase, otherwise 0
  */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
